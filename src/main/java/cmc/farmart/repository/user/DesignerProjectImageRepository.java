package cmc.farmart.repository.user;

import cmc.farmart.entity.designer.DesignerProjectImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DesignerProjectImageRepository extends JpaRepository<DesignerProjectImage, Long> {
}
