package cmc.farmart.repository.user;

import cmc.farmart.entity.designer.DesignerProject;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DesignerProjectRepository extends JpaRepository<DesignerProject, Long> {
}
